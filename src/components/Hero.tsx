import { VoxelLaptopScene } from "./VoxelLaptopScene";

export const Hero = () => {
  return (
    <section
      id="home"
      className="relative isolate h-[100svh] min-h-screen w-full overflow-hidden bg-[#f0f0f0] text-black"
    >
      <VoxelLaptopScene />
    </section>
  );
};
